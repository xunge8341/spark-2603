# Benchmark artifacts

Machine-readable performance artifacts are generated here by `scripts/perf_report.sh`:

- `benchmark/reports/perf_report.json`
- `benchmark/reports/perf_report.csv`

These files are intended for CI diff/comparison and are not committed by default.
