[package]
name = "spark-metrics-prometheus"
version = "0.1.0"
edition.workspace = true
license.workspace = true

[dependencies]
# Counters live in spark-transport; exporter is leaf-only.
spark-transport = { path = "../spark-transport" }
spark-uci = { path = "../spark-uci" }
